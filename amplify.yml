version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - echo "No build step required for this project"
  artifacts:
    baseDirectory: /public/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*