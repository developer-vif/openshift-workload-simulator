version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No dependencies to install"
    build:
      commands:
        - echo "No build step required"
  artifacts:
    baseDirectory: .
    files:
      - index.html
      - style.css
      - simulator.js
      - script.js