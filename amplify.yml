version: 1
appRoot: . # The root of your application
backend:
  phases:
    build:
      commands:
        - '# The build commands for your backend'
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - echo "No build step required for this project"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*